# Optional Firebase Hosting config
# Do NOT deploy until the correct Firebase Project ID is confirmed.
# Recommended Project ID candidate: sotong-site-manager
#
# When ready:
#   1. firebase login
#   2. firebase use <confirmed-project-id>
#   3. flutter build web --release
#   4. firebase deploy --only hosting

# Placeholder — replace after project confirmation.
# {
#   "hosting": {
#     "public": "build/web",
#     "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
#     "rewrites": [{ "source": "**", "destination": "/index.html" }]
#   }
# }
